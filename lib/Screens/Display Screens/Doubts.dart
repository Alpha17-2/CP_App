import 'package:flutter/material.dart';

class Doubts extends StatelessWidget {
  static const routeName = '/doubts';
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Doubts'),
      ),
    );
  }
}
