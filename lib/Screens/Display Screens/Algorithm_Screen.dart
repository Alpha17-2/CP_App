import 'package:flutter/material.dart';

class Algorithm extends StatelessWidget {
  static const routeName = '/algo';
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Algorithms'),
      ),
    );
  }
}
